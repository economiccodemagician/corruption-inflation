# Corruption and Inflation Dashboard

## Run locally

```bash
python -m pip install -r requirements.txt
streamlit run app.py
```

The dashboard reads:

`outputs/analysis_ready_corruption_inflation.csv`

Keep `app.py`, `requirements.txt`, the notebook, and the `outputs` folder in
the project root.
